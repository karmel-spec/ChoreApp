{
  "name": "family-chore-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Family chore dashboard prototype prepared for Netlify deployment.",
  "scripts": {
    "build": "node scripts/verify-static-site.mjs",
    "dev": "python3 -m http.server 4174",
    "deploy:netlify": "netlify deploy --prod --dir=outputs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1",
    "twilio": "^6.0.2",
    "web-push": "^3.6.7"
  }
}
